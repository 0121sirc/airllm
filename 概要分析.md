# AirLLM 项目概要分析

## 1. 项目定位

**AirLLM**（v3.1.0，Apache 2.0，作者 Gavin Li）是一个"极低显存大模型推理"框架，核心思路是**不做量化/蒸馏/剪枝**，仅靠**逐层权重流式加载（layer-wise streaming）**，把巨型模型跑在小显存设备上：

- 70B Llama 3.x 可跑在 **4GB** 单卡上
- 405B Llama 3.1 跑在 **~8GB**
- 671B DeepSeek-V3（FP8）跑在 **~12GB**
- 2.8T Kimi K3（稀疏 MoE）跑在 **~4GB** 以内

同时支持 CPU 推理与 macOS（MLX 后端）。模型代码与推理逻辑完全交给 Hugging Face `transformers` 负责，因此**新架构发布当天即可支持**。

## 2. 核心原理

1. **权重分片**：首次加载时把整个 checkpoint 按模块拆分为"一层一个 safetensors 分片"（embed、每个 decoder layer、norm、lm_head），落盘缓存，之后直接复用。
2. **meta 设备建模**：用 `accelerate.init_empty_weights` 在 `meta` 设备上实例化真实 `transformers` 模型（不占显存），由 transformers 驱动完整 forward/generation 逻辑。
3. **forward hook 流式传输**：为 embed → layers → norm → lm_head 的每个大模块挂 `forward_pre_hook`/`forward_hook`，在模块执行前把权重从磁盘搬到 GPU，执行完立即释放回 meta 并 `clean_memory()`。**任何时刻 GPU 上只有一个层**，显存需求取决于单层大小而非模型总量。
4. **预取（prefetching）**：用单 worker `ThreadPoolExecutor` 在后台预载下一层，使磁盘加载与当前层计算重叠（约 10% 提速）。
5. **逐专家流式（per-expert streaming）**：针对稀疏 MoE（Kimi K3 每层 896 个专家、token 只路由 16 个），借助 safetensors 的 seek 能力只读"被路由到"的专家张量，把"整层 ~55GB 展开"降为"每次 ~1GB 有效加载"。
6. **预量化模型支持**：FP8 / MXFP4（compressed-tensors）checkpoint 直接以压缩载荷搬运（PCIe 少搬 4 倍数据），在 GPU 上按需解压；bitsandbytes 4bit(NF4)/8bit 块状量化用于压缩模式。

## 3. 目录与模块结构

```
air_llm/
├── airllm/                      # 核心推理包
│   ├── airllm_base.py           # ★ AirLLMBaseModel：分片+meta建模+hook流式核心
│   ├── auto_model.py            # AutoModel 工厂：按 config.architectures 选类
│   ├── airllm.py                # AirLLMLlama2（通用 Llama 系）
│   ├── airllm_chatglm.py        # ChatGLM（transformer.encoder.layers 布局）
│   ├── airllm_qwen.py           # Qwen（transformer.h 布局）
│   ├── airllm_qwen2.py          # Qwen2
│   ├── airllm_baichuan.py       # Baichuan（自定义 tokenizer）
│   ├── airllm_internlm.py       # InternLM
│   ├── airllm_mistral.py        # Mistral
│   ├── airllm_mixtral.py        # Mixtral
│   ├── airllm_kimi_k3.py        # Kimi K3（多模态 MoE，resident 模块+专家流式）
│   ├── airllm_llama_mlx.py      # macOS 专用纯 MLX 实现（自建 Llama 网络）
│   ├── utils.py                 # ★ 拆分/加载/压缩/链接 等工具
│   ├── profiler.py              # 分层耗时/显存分析器
│   ├── tokenization_baichuan.py # Baichuan tokenizer 修复版
│   └── persist/
│       ├── model_persister.py           # 抽象基类 + 平台选择
│       ├── safetensor_model_persister.py # Linux/CUDA 后端（*.safetensors + .done 标记）
│       └── mlx_model_persister.py        # macOS 后端（*.mlx.npz，torch→mlx 命名映射）
├── setup.py                     # PyPI 打包（依赖已 pin 到测试过的范围）
├── inference_example.py         # 单文件推理示例
└── tests/
    ├── test_automodel.py        # AutoModel 架构→类映射
    ├── test_compression.py      # 4bit/8bit 压缩往返正确性
    ├── test_streaming_gpu.py    # 手动 GPU 压测（可模拟小显存卡/对比全量加载）
    └── test_kimi_k3_split.py    # K3 型 checkpoint 拆分结构测试（link 直通等）

training/       # QLoRA 微调（Anima 模型），run_*.sh 脚本
rlhf/           # DPO RLHF 训练（qlora_dpo.py），含效果对比 csv/图
anima_100k/     # 长上下文训练与评测数据集 + longer_training.py + flash_llama
examples/ eval/ # Jupyter 示例（Colab 推理、Elo 评测）
scripts/        # star history 生成等
.github/workflows/  # release.yml（PyPI 发布）、star-history.yml
```

## 4. 关键设计要点

- **分层架构**：`AutoModel.from_pretrained(...)` → `auto_model.py` 读取 config 架构名，查 `ARCH_OVERRIDES` 表，命中则用专用子类，否则回退通用 `AirLLMBaseModel`，从而对新架构零改动。
- **可扩展钩子**：子类只需覆盖 `set_layer_names_dict()` 声明 embed/layer_prefix/norm/lm_head 的路径，兼容非标准布局（K3 的 `language_model.model.*`）与 resident 模块（vision tower、projector 常驻不流式）。
- **深模型 dtype 保护**：默认用模型的 bf16（而非 fp16），避免 94 层等深模型溢出产生垃圾输出。
- **磁盘省空间策略**：
  - `link_or_copy_file`：优先硬链接（再软链、最后复制）复用原 checkpoint 数据；
  - 已是"一层一文件"的 checkpoint（如 K3 每层一个 ~17GB 分片）直接 passthrough 链接、不重复写 1.5TB；
  - `delete_original` 拆完即删原文件，省一半空间。
- **健壮性**：`import airllm` 永远可用——可选依赖（bitsandbytes、compressed-tensors、sentencepiece）缺失时逐类降级并告警；`init_model` 对 sdpa 失败回退 eager；对旧 transformers 移走的符号做兼容再导出。
- **生成能力恢复**：对不继承 `GenerationMixin` 的远程代码模型，动态混入基类，并 patch `device`/`dtype` 属性让 transformers 生成工具正确放置张量。

## 5. 技术栈与依赖

- torch ≥2.4、transformers ≥4.49（<5.13）、accelerate、safetensors、huggingface-hub
- 可选：bitsandbytes（4bit/8bit 压缩）、compressed-tensors（MXFP4）、flash-attn（K3 强制）、mlx（macOS）
- 明确不强制量化，压缩模式为"减少磁盘加载字节数"的提速手段，与 activations 量化无关，精度损失极小。

## 6. 已知特性与注意点

- 推理前首次会把模型拆层落盘，需要充足磁盘空间（元数据 `MetadataIncompleteBuffer` 错误多为磁盘满）。
- 压缩模式暂不支持 prefetching；拆层是 CPU 密集的磁盘操作，大模型耗时较长（K3 级别需数小时级）。
- gated 模型需 `hf_token`；部分 tokenizer 无 pad token，需关闭 padding。

## 7. 评价与局限

**优点**：理念极简（meta 设备 + hook 流式）、对 transformers 生态复用度高、支持模型面广、兼顾了磁盘/显存/带宽三种稀缺资源的优化，且对多模态与巨型 MoE 有专门处理。

**局限**：本质是"以磁盘 I/O 换显存"，吞吐受限（逐 token 需遍历全部层分片）；模型越大拆层与预取对磁盘性能越敏感；仅推理场景（无训练）；MLX 路径为独立实现、能力面（压缩/专家流式等）明显窄于主路径。
