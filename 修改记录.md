# 修改记录

本文档记录针对 AirLLM 在 **RTX 3060 6GB** 上跑通并优化 **Qwen3-30B-A3B-Instruct**（MoE，每层 128 专家 / top-8）所做的一系列修改：每处修改的位置、改动内容、实现思路与收益。

环境：Python 3.11.15 · torch 2.13.0+cu130 · transformers 4.56.2 · bitsandbytes 0.50.1 · accelerate · safetensors 0.8.0。

---

## 1. 新增 Qwen3 支持（逐专家流式）

### 文件
- 新增 `air_llm/airllm/airllm_qwen3.py`
- 修改 `air_llm/airllm/__init__.py`（注册 `AirLLMQwen3`）
- 修改 `air_llm/airllm/auto_model.py`（`Qwen3ForCausalLM` / `Qwen3MoeForCausalLM` → `AirLLMQwen3`）

### 实现思路
Qwen3 密集版与 MoE 版共享同一 `layer_prefix` 布局，但 MoE 版的每个专家是独立子模块：

```
model.layers.<i>.mlp.gate         # 路由器
model.layers.<i>.mlp.experts.<e>  # 128 个 Qwen3MoeMLP
```

`AirLLMQwen3.set_layer_names_dict()` 仅需额外声明 `expert_prefix: 'mlp.experts'`。基类的 `_setup_expert_streaming()` 扫描每个层分片里 `mlp.experts.<数字>.` 前缀的张量，按专家编号分组，并为每个专家模块挂独立的 forward pre/post hook——token 路由到哪个专家，才加载哪个专家。

### 收益
- 一个 30B MoE 层的分片展开约 ~1.1GB，但单 token 只路由 8/128 个专家，逐专家流式把"每层搬 1.1GB"降为"只搬被路由到的 ~8 个专家（每个几百 KB）"。
- 与框架对 Kimi K3 的既有思路一致，Qwen3 复用同一套 hook 机制，无需改动 transformers 模型代码。

---

## 2. 4bit(NF4) 压缩 + 逐专家流式打通

### 文件
- `air_llm/airllm/airllm_base.py`

### 改动
- 新增 `_maybe_decompress(state_dict)`：压缩模式下调用 `uncompress_layer_state_dict()`（来自 `utils.py`），把从分片读出的 packed 4bit/8bit 张量 + 量化状态还原成普通权重。
- `_expert_pre_hook()`：从"读子集后直接上设备"改为"读子集 → `_maybe_decompress` → 上设备"。
- `_load_streamed_layer(idx, decompress=True)`：整层/非专家子集路径同样接上 `_maybe_decompress`。

### 实现思路
`split_layers` 压缩拆层时，量化后的载荷（含 `.4bit.absmax` / `.4bit.code` 等伴生张量）与普通张量按相同 key 命名落在分片里。逐专家流式此前只支持未压缩分片，压缩分片读出的专家张量是 packed 数据，直接塞给模型会得到垃圾输出，因此必须在 hook 里先反量化。

### 收益
- 磁盘读取字节数降到约 1/4（4bit 每个参数 0.5 字节 vs bf16 2 字节），PCIe/磁盘 I/O 压力大减；4bit 分片仅 ~16GB（bf16 拆层 ~57GB）。
- 是后面所有性能数字的前提。

---

## 3. 压缩模式下恢复 prefetching

### 文件
- `air_llm/airllm/airllm_base.py`

### 改动
- `__init__`：删掉"压缩模式下强制禁用 prefetching"的分支。
- `_load_streamed_layer(idx, decompress=True)`：prefetch 提交时传 `decompress=False`（`self._executor.submit(self._load_streamed_layer, nxt, False)`）。
- `_pre_hook`：消费预取结果后在**主线程** `_maybe_decompress`。

### 实现思路
框架原来禁止压缩 + 预取，原因应是后台线程解压会碰 CUDA。改为：**后台 worker 只做 CPU 裸读（safetensors mmap 读入原始 packed 张量），解压留在主线程**，既保留加载/计算重叠，又规避线程 CUDA 上下文问题。

### 收益
- 实测在句柄缓存（见下）之后，预取开/关差距极小（98.5s vs 96.7s，噪音内）——读取已足够快，预取红利被句柄缓存取代；但开启预取不引入正确性问题，保留默认开启。

---

## 4. safetensors 句柄缓存

### 文件
- `air_llm/airllm/utils.py`

### 改动
- 新增模块级 `_safe_open_cache = {}`。
- `load_layer_subset()`：按分片路径缓存 `safe_open` 句柄，复用读多个张量，不再每次 `with safe_open(...)` 重开。

### 实现思路
一个层分片的 safetensors **JSON 头约 204KB、1572 个 key**，每次 `safe_open` 都要完整解析一遍（实测 ~2.7ms），而真正 `get_tensor` 的 mmap 切片读取只有 ~0.1ms。逐专家流式一个 token 要读上千次专家子集，重开句柄的开销淹没一切。分片拆完后不可变，句柄长期持有安全；多个线程并发 `get_tensor` 只读同一 mmap，也安全。

### 收益
- 专家读取 2.8ms → ~0.1ms，是当前最大单项提速：**158.3s → 98.5s（64 tokens）**。

---

## 5. clean_memory 瘦身 + 逐层 hook 不再 GC

### 文件
- `air_llm/airllm/utils.py`（`clean_memory()`）
- `air_llm/airllm/airllm_base.py`（`_post_hook`、`_expert_post_hook`）

### 改动
- `clean_memory()`：移除 `gc.collect()` 和 `malloc_trim(0)`，只保留 `torch.cuda.empty_cache()`。
- `_post_hook`：删除每层结束后对 `clean_memory()` 的调用。

### 实现思路
cProfile 显示每层 `_post_hook` 里的 `gc.collect()` 单次 ~300ms，占单 token 总时长近一半（26.5s 中 15.38s）。权重对象靠 CPython 引用计数即时释放，流式框架持有的张量集合有界，无需全量 GC；CUDA caching allocator 会把释放的块复用给下一层。`malloc_trim` 是给进程堆的，对 torch CUDA 缓存无意义。

### 收益
- GC 从 15.38s/层组 降到 ≈0，是 4bit 首跑"卡住"假象的根因修复；直接带来 **1289s → 158.3s** 量级的变化。

---

## 6. check_space 4bit 公式修复

### 文件
- `air_llm/airllm/utils.py`

### 改动
- 4bit 预估分片大小：`int(size / 0.2813)` → `int(size * 0.2813)`。

### 实现思路
原代码把 4bit 分片大小误估成原大小的 **3.55 倍**（`/0.2813`），压缩模式下拆层前的空间校验几乎必然失败（57GB 原文件被估成 202GB）。NF4 每参数 ~0.5 字节、bf16 每参数 2 字节，故乘 `0.2813`（≈0.5/2）。

### 收益
- 压缩拆层路径恢复可用，4bit 分片约 16GB。

---

## 7. 性能数据（Qwen3-30B-A3B-Instruct，RTX 3060 6GB，5GB VRAM 上限）

| 配置 | 64 tokens | tok/s | 峰值 VRAM |
|---|---|---|---|
| bf16 拆层（原始逐层流式） | 1289s | 0.05 | ~814MB |
| 4bit + 逐专家流式（含 GC 修复） | 158.3s | 0.40 | ~814MB |
| + safetensors 句柄缓存 | **98.5s** | **0.65** | ~814MB |

- 累计约 **13x 提速**（1.54s/token）。
- 输出与全精度一致（贪婪解码）。

---

## 8. 下一步可探索方向（未实施）

1. **非专家权重常驻 GPU**：48 层 attention 权重每层 ~9.7M 参数（占非专家子集的 99.98%），每个 token 都重读+反量化+上设备再逐出。若反量化后一次性常驻（~1GB bf16，5GB 预算富余），每 token 只流式专家，预期 1.54 → ~0.9s/token。
2. **确定性专家预取**：包装 `Qwen3MoeSparseMoeBlock.forward`，路由算完即可拿到精确 `expert_hit`（100% 命中），后台线程反量化专家 2..8 与专家 1 计算重叠（CUDA event 同步），预期再省 ~0.4s/token。
3. **mmap 免 clone 读取**：跳过 `get_tensor` 的拷贝（每 token ~150MB），预期省 ~0.2-0.3s/token。
4. **NF4 直接反量化到 bf16**：跳过 fp32→bf16 转换内核。

---

## 9. OpenAI 兼容 API 服务（`air_llm/openai_api_server.py`）

给 AirLLM 加一层 OpenAI 兼容的 HTTP 接口，供 `openai` SDK 等任意客户端直接连，不用改客户端代码。服务在单进程内加载一个模型，AirLLM 逐层/逐专家流式决定了同一时刻只能跑一次生成，因此所有请求通过一个全局锁串行化。

### 端点

| 端点 | 说明 |
|---|---|
| `GET /v1/models` | 返回已加载模型 id |
| `POST /v1/chat/completions` | 标准聊天补全，支持 `stream`（SSE）与 `[DONE]`、`usage` |

### 请求参数 → `generate` 映射

- `messages` → `tokenizer.apply_chat_template(..., add_generation_prompt=True)`；`max_tokens` → `max_new_tokens`；`temperature>0` → `do_sample=True`，否则贪婪；`top_p/top_k/presence_penalty/frequency_penalty/seed` 直通；`stop`（字符串/列表）→ `stop_strings`（transformers 4.56 要求同时给 `generate` 传 `tokenizer`，否则报 "could not locate a tokenizer"，已处理）；`n>1` → `num_return_sequences`（仅非流式）。

### 上下文长度与多轮裁剪

- **上下文窗口**：`--max-seq-len`（默认 **4096**）决定 prompt 截断点与单次 `max_new_tokens` 上限（`max_seq_len - prompt_len`）。模型 `max_position_embeddings=262144` 无需 rope，调到 16384 时 KV cache 约 1.5GB（bf16 96KB/token），加 embed/lm_head 瞬时 ~0.62GB 及激活，峰值 ~2.5–3GB，6GB 卡可行；调大无需重拆分片。
- **多轮裁剪**：`tokenizer` 的 `truncation=True` 是"保头去尾"，会砍掉最新对话。新增按轮次窗口：保留开头的 system 消息 + 最近 N 轮（`(user,assistant)` 对，末尾未答复的 user 消息一并保留），由 `--max-rounds`（默认 4）或请求体 `max_rounds` 控制，`max_rounds=0` 关闭。之后再交给 tokenizer 截断兜底超长单轮。

### 启动用法

```bash
# 无网环境用本地快照路径（分片已存在时秒级复用，不重新拆层）
.conda_env/bin/python air_llm/openai_api_server.py \
  --model "$HOME/.cache/huggingface/hub/models--Qwen--Qwen3-30B-A3B-Instruct-2507/snapshots/"*/ \
  --shards-path /home/kg/airllm_shards \
  --compression 4bit \
  --max-seq-len 4096 --max-rounds 4 \
  --port 8000
```

### 客户端连接

```python
from openai import OpenAI
client = OpenAI(base_url="http://127.0.0.1:8000/v1", api_key="sk-dummy")  # 服务不校验 key
resp = client.chat.completions.create(
    model="任意/可空",  # 服务忽略，固定用已加载模型
    messages=[{"role": "user", "content": "1+1=?"}],
    max_tokens=16, temperature=0, stream=True,
)
```

### 验证结果

- 非流式、SSE 流式（增量 + finish_reason + usage + `[DONE]`）、并发两请求串行化均通过。
- `stop` 字符串正常截停（内容停在匹配处，`finish_reason=stop`）。
- 6 轮对话 + `max_rounds=2`：保留 system+最近 2 轮+末轮提问（prompt 69 tokens），正确答出最后话题，早轮不串扰。

---

## 附：相关文件

- 推理/压测脚本：`run_moe_inference.py`（`--compression 4bit`，默认开启 prefetching）
- OpenAI 兼容服务：`air_llm/openai_api_server.py`（依赖 fastapi/uvicorn，已装入 `.conda_env`）
- 4bit 分片目录：`/home/kg/airllm_shards/splitted_model.4bit/`（~16GB，可 ~90s 重建）
- 运行环境变量：`HF_ENDPOINT=https://hf-mirror.com`、`HF_HUB_DISABLE_XET=1`